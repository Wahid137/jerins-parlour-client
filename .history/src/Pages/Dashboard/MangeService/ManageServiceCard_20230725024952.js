import React from 'react';

const ManageServiceCard = ({ name, description, image }) => {
    return (
        <div>
            <h2>This is manage Card</h2>
        </div>
    );
};

export default ManageServiceCard;